# D02 診断用スナップショット

このブランチは、MotorGameV3 P3-4 G8人間試遊で発見されたD02表示不具合を、
ローカル作業ツリーへアクセスできないデバッガーへ渡すための一時スナップショットです。
`main`/`develop`への統合、production反映、tag、releaseを目的としません。

## 再現条件

- 電池: リチウムポリマー
- ブラシ: 貴金属
- 巻数: 15回
- 研磨: 1.0
- ブラシ圧: 1.0
- スリット幅: 0.05 mm
- 実験室 → 車輪を浮かせる → 始動

## 期待結果

- 約299物理step（約2.5秒）でD02コイル焼損が`destructionTerminal`になる
- 終端前に白→灰→黒の煙が表示され、発煙SEが鳴る
- 検死結果のコイル発熱ゲージが100.0%になる

## 現在の結果

- D02終端停止は発生する
- 計測パネルの電池発熱ゲージは0%のまま
- 煙が表示されない

計測パネルの値は`batteryHeat`であり、D02が判定に使う
`coilHeatGaugeRatio`とは別物です。電池発熱ゲージ0%を理由に物理パラメータや較正値を
変更しないでください。

## 調査仮説（未確定）

1. motor-only経路では`_elapsedSec`が進む一方、`RaceEffects`が
   `vehicleState.elapsedTimeS`を演出tickに使っているため、粒子時計が進まない。
2. D02終端stepで`_runAccumulator`が描画前にnull化され、終端イベントを
   `RaceEffects`が読む前に粒子が消去される。

仮説を前提に断定せず、実コードとテストから確認してください。

## 依頼する成果物

1. 原因
2. 既存の演出配線内で閉じる最小修正案
3. 変更対象ファイル
4. unified diff形式のパッチ
5. 必要なテスト変更
6. 実行できなかった確認事項と既存仕様への影響

## 制限

- 物理パラメータ、較正値、公開契約を変更しない
- 新規基盤、依存パッケージ、UI全体のレトロ化を追加しない
- この診断ブランチからcommit、push、PR、merge、tag、releaseを行わない
- 正式な試遊条件は`docs/phase3-p3-4-g7-playtest-sheet.md`を参照する
