# P4-1C R2-SWEEP-2停止・R2-SWEEP-3 arbiter正式裁定

受領日時: 2026-08-31T04:56:04Z  
回答者: `arbiter_mot3`  
基点: `874f8dd9e6407bdc02a04fda2c862394bc06d069`  
状態: **正式回答・人間再承認受領済み。P41C-R2-SWEEP-3を解禁**

以下はagmsgで受領した正式回答全文である。

---

【arbiter_mot3 正式回答／P4-1C R2-SWEEP-2停止裁定】

## 0. 受領確認と、arbiter自身の誤りの記録

報告全文を読了。まず記録すべきは私の側の誤りである: **裁定2(B)の「motor-only定常ω≈187.5 rad/sで交差しうる」という概算は、承認済み固定構成(P4-0シナリオ由来、coilTurns=30)ではなく、R2-A fixtureの別構成(coilTurns=80・3.0V・磁石5mm・低ブラシ圧)の実測値に依拠していた。** aliceの報告は構成を限定して書いており、構成の同一性を検証せず概算に使ったのは私である(検査する側も検査される——本件は裁定の方向〈motor-onlyがD01の機構文脈〉の誤りではなく、承認済みfixture構成でその機構域に届くかの検証を欠いた見積り誤りとして、台帳の本裁定エントリへ1行記録すること)。K_LOOSE=0.5で余裕−6.999でも発火しない機構(過渡ピーク13frame≪必要360frame、定常ωは全域で閾値を30〜76 rad/s下回る)は報告の実測どおりであり、検算上の矛盾はない。

## 裁定1(停止受入): **受け入れる。正当な負の結果である。**

2570走行・打ち切りなし・陽性対照3点・意図外発火0・第二入力新設なし(初速はproduction既存2定数の使い分けであり新設に当たらない、という整理も正しい)。初速15 rad/sでは既定素材のコギングを越えられず始動しないという発見も、正直に記録された有用な事実である。

## 裁定2(次工程): **「成立条件だけを調べる追加read-only探索」→「成立すれば構成を移した再sweep」の二段を採る。C2の即時延期・不採用はまだ選ばない(ただし明示的な失敗出口として定義する)。**

物理的な実態はこうである: **D01(高回転崩壊)は「回るビルド」の現象である。** P4-0固定構成は10mを20〜30秒で走る意図的に弱いビルドであり、これが自壊回転域へ届かないのは実物どおりの正しい物理である。一方、R2-A構成(80ターン・3.0V級)は定常187.5 rad/sに達する——**production妥当域内に、閾値域へ届く「熱いビルド」は存在しうる**。したがってC2の較正対象は「固定の弱いビルドを光らせること」ではなく「熱いビルドにおいて、緩い巻きが締まった巻きより早く崩れること」である。

**次のexact有限delta(P41C-R2-SWEEP-3、人間再承認対象)**:

- **第1段(成立条件調査、read-only)**: production妥当域内の既存パラメータのみ(coilTurns・batteryVoltage・magnetDistanceMm・brushPressure等の有限少数格子、script-local)で、motor-only定常ωが「緩め閾値域(K_LOOSE格子で到達可能な104.7〜188.5 rad/s)に入り、かつ締め閾値209.440を超えない」構成が存在するかを実測する。軸を同時に増やさず、有限列挙で行う。
- **第2段(成立時のみ)**: 特定した構成でfixture record(ターン数・張力パターンを当該構成へ整合、正規command/reducer経路で生成)を組み直し、**式・K_LOOSE格子・COIL_DEFORM_FRAMES・素材リスト・seedを無変更**のまま257点×5候補を再実施する。
- **受入基準(三側拘束)**: (i)熱いビルドで、緩い張力ほど早期発火の単調差。(ii)同ビルドでmeanTension=1は非発火(締めた巻きは崩れない)。(iii)弱いビルド(P4-0固定構成)+NORMAL_OPERATION 15組合せは張力によらず非発火——第1回1285走行・第2回2570走行の結果を(iii)の証跡としてそのまま採用する(2回のsweepは無駄になっていない)。
- **失敗出口(明示)**: 第1段でproduction妥当域内に該当構成が存在しない場合、**C2(張力→D01接続)は非採用とし、H4の危険側はC3(破断)とP4-1F(被膜/D10)へ全面委譲する**。これはゲーム性の欠落ではない——「切れないよう抑える」緊張の主担体はもともと工程内のC3であり、D01は高回転ビルド文脈の追加リスクである(裁定3参照)。

## 裁定3(変更禁止境界)

- **変更禁止を維持**: 式(mean=1で閾値不変を含む)・K_LOOSE格子{0.1..0.5}・**COIL_DEFORM_FRAMES=360**(3.0秒連続——過渡ピークを発火扱いにするための短縮は、通常操作の瞬間的な回転尖りを誤爆させる過剰較正であり明示的に禁止する)・素材リスト・正規record生成経路・seed・dt。
- **本deltaで変更可(人間承認対象)**: fixture構成(coilTurns等、production妥当域内)・fixture recordのターン数と張力パターン・初速(第2回の実測に基づきMAX_FLICK_OMEGA=40を既定とし、15は「始動しない」事実の記録として残す)。
- 参考値(K≥0.709等)の候補昇格禁止は前裁定どおり維持。

## 裁定4(張力リスクをD01だけへ背負わせるべきか——Suu照会の最終点): **背負わせるべきではない。**

H4の危険側は設計上2チャネルであり、(i)C3破断=巻き工程内の主リスク(全ビルド共通・「釣りの巻き上げ」の緊張の本体)、(ii)D01耐性低下=高回転ビルド限定の追加リスク(ビルドを攻めるほど巻きの質が問われる、という正しいスケーリング)。C2が失敗出口に入ってもH4のゲーム性はC3で成立する。この2チャネル構造を人間再承認文とdocsへ明記し、「D01が弱いビルドで発火しないのは欠陥ではなく物理である」ことを将来の読者に残すこと。

## 人間再承認文(案)

> P41C-R2-SWEEP-3を承認します。第1段: production妥当域内の既存パラメータの有限列挙で、motor-only定常ωが緩め閾値域(104.7〜188.5 rad/s)に入り締め閾値209.440を超えない構成の存在をread-only実測する。第2段(成立時のみ): 当該構成のfixtureで式・K_LOOSE格子・COIL_DEFORM_FRAMES=360・素材リスト・seed無変更の257点×5候補を再実施し、三側拘束(熱いビルドの単調早期発火/締め非発火/弱いビルド・NORMAL全域非発火〈既存2回のsweepを証跡採用〉)で判定する。第1段で該当構成が存在しない場合、C2は非採用としH4危険側はC3/D10へ委譲する。COIL_DEFORM_FRAMESの短縮・式改造・参考値昇格は禁止。arbiter概算誤り(R2-A構成と固定構成の混同)の経緯を台帳へ記録する。

C2 production係数・R3・spec/art-spec・commit/tag/push/deploy/PR/mergeの禁止維持に同意する。arbiter_mot3はコード・docs編集・Git操作・新式や新係数の発明を行っていない(SWEEP-3の構成調査は既存production妥当域の探索であり、新パラメータの発明ではない)。

以上、正式回答として全文送信し、以後停止する。

---

## 人間再承認要件

上記「P41C-R2-SWEEP-3」全文の人間再承認が必要である。承認前は第1段の成立条件調査へ進まない。C2 production係数、R3、spec/art-spec、commit、tag、push、deploy、PR、mergeは引き続き禁止する。

## 裁定台帳エントリ

2026-08-31、arbiterのR2-SWEEP-2裁定2(B)は、R2-Aテスト構成(coilTurns=80・3.0V・磁石5mm・低ブラシ圧)の定常ω約187.5 rad/sを、P4-0固定構成(coilTurns=30)へ誤って適用した概算だったと訂正した。motor-onlyという機構文脈の方向は維持するが、承認済みfixture構成が同じ回転域へ届くことを検証しなかった見積り誤りとして記録する。以後、実測値には構成・helper由来・正典構成か否かを同じ行で明示する。

## 人間再承認記録

2026-08-31、人間プロジェクトリードが次の全文でP41C-R2-SWEEP-3を承認した。

> P41C-R2-SWEEP-3を承認します。第1段: production妥当域内の既存パラメータの有限列挙で、motor-only定常ωが緩め閾値域(104.7〜188.5 rad/s)に入り締め閾値209.440を超えない構成の存在をread-only実測する。第2段(成立時のみ): 当該構成のfixtureで式・K_LOOSE格子・COIL_DEFORM_FRAMES=360・素材リスト・seed無変更の257点×5候補を再実施し、三側拘束(熱いビルドの単調早期発火/締め非発火/弱いビルド・NORMAL全域非発火〈既存2回のsweepを証跡採用〉)で判定する。第1段で該当構成が存在しない場合、C2は非採用としH4危険側はC3/D10へ委譲する。COIL_DEFORM_FRAMESの短縮・式改造・参考値昇格は禁止。arbiter概算誤り(R2-A構成と固定構成の混同)の経緯を台帳へ記録する。

この承認で解禁するのはP41C-R2-SWEEP-3の二段read-only探索だけである。C2 production係数、R3、spec/art-spec、commit、tag、push、deploy、PR、mergeは禁止を維持する。
