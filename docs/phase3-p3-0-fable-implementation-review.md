# Fable最終レビュー: P3-0実装完了報告

出所: 2026-08-03、人間PMからSuu_mot3へ提示され、agmsgでalice_mot3へ全文中継された正式回答。

> 以下は人間PMが提示したFable回答の逐語記録である。Fable名義の内容をエージェントが生成・補筆したものではない。

## 判定

**commit可(条件付き承認)**

## 判定の根拠

(1) 計画v7が私の必須修正P1(4.5節新設)・P2(Q7遡及申告と12節訂正)・軽微条件4点・Q1〜Q7裁定のすべてを正確に反映していることを本文で確認した。(2) 改訂UI v5が正式回答のM1/M3/M5(i)(ii)/C4/C7・P1/Q3/Q4aを正確に反映し、テスト方針11節が各項目に対応するテストを個別に列挙していることを確認した(軽微条件(4)はこれで解消)。(3) 実装対応表(報告3節)の全項目が裁定原文と一致する。(4) テスト全出力(67ファイル/1095件、省略なし)・build・lint・cmp AGENTS.md CLAUDE.md差分ゼロ・git diff --check問題なし。(5) サブステップごとのSuu_mot3独立検証と、サブステップ2(6点)・3(10→7→5点の3ラウンド)の独立コードレビューによる是正が、裁定の意味論を変えずに実装欠陥のみを潰していることを是正内容の記述から確認した。特に是正#1(図鑑初回登録報酬の未結線)はspec §5.1/§7.5の要求そのものであり、検出は正当かつ必須だった。

## commit承認の条件

### 条件1(人間PMの明示判断事項)

commit範囲へのdocs包含の決定。報告6節はdocs配下の全ファイルを「本実装差分と無関係」としてcommit範囲から除外しているが、docs/phase3-plan-v12.md・docs/phase3-p3-0-plan.md(v7)・docs/phase3-ui-autopsy-plan-v5.mdおよび裁定記録は無関係ではなく、このcommitが実装する契約そのものである。「決定は文書に記録されて初めて存在する」原則、およびFable可搬性ゲート(新しいFableがリポジトリのみで立ち上がれること)の観点から、統治文書を実装と同一commit(または直後の別commit)へ含めることを推奨する。含めない判断も可能だが、その場合は人間PMの明示的な決定として記録すること。過去版(v1〜v11等)の扱いも同時に決めてよい(履歴として含める/除外いずれも可、決定の記録が要件)。

### 条件2(人間承認時の明示認知)

PROVISIONAL_DISCOVERY_REWARD_G = 500。この値は実装中にSuu是正で新設された経済に触れる仮値であり(新規発見×500Gの現金加算、初期所持金1000Gに対して有意)、計画の事前承認には含まれていなかった。仮値としての扱い(専用定数・UI注記「試遊用の仮値」・Phase 5で数値保証)は正しく、値自体に異議はないが、commit承認時に人間PMがこの値を明示的に認知したことを記録すること。

### 条件3

commit後、従来どおりgit tagを付与すること(例: p3-0-complete)。

## 推奨(条件ではない)

pitfalls登録2件: (a) alice_mot3がAgentツールのmodel:"fable"委任で自己生成した文書が一時、正式Fable回答として扱われた事象(UI v5冒頭注記)。検出・照合・撤回の対応は適切だったが、再発防止として「Fable回答の真正性は人間PM(またはSuu_mot3の中継記録)経由のもののみを正とし、エージェントがFable名義の文書を生成することを禁じる」を番号付きルールとして次のdocsゲートでAGENTS.md/CLAUDE.mdへ追加することを推奨する——散文でなく番号付きルールにするのは、番号付きルールのみが中継で100%伝達されるというこのプロジェクト自身の構造法則による。(b) 単一tsconfigの型検査境界によりWearState破壊的変更が計画未記載ファイルへ波及した事象と「所有境界つき依存閉包」による解決——今後の破壊的型変更の計画時に依存閉包を事前列挙する教訓として。

バンドルサイズ監視: 781KB(gzip 219KB)はCLAUDE.md非機能要件の1MB予算に対し余裕が縮んでいる。P3-1以降で増加が続くため、各ゲート報告にバンドルサイズを含めることを推奨する。

P3-1計画への申し送り: Q6不変条件(「advanceDestructionStateは差分換算が実装済みのモードのイベントしか発行してはならない」)の実コード検証テストはP3-1計画のDoDに必須で含めること(計画に明記済みだが、P3-1計画レビューで最初に確認する)。A1(給電停止機構の要否再監査)もP3-1計画の必須項目である。

## 総合

P3-0の実装は計画・裁定・正典と整合しており、条件1〜3の処理をもってcommit・tag・人間承認へ進んでよい。P3-0(クロスレイヤ契約の型凍結ゲート)はこれで完了となり、次はP3-1(D01/D03、契約の最小実証)の実装前ステップ計画レビューである。
