# P4-1C R1/R2 有限バンドル・arbiter再判定

受領日: 2026-08-30  
回答者: `arbiter_mot3`  
基点: `874f8dd9e6407bdc02a04fda2c862394bc06d069`  
状態: **正式回答受領済み。人間再承認前のためproduction/test実装、較正値変更、Git操作は禁止**

以下はagmsgで受領した正式回答全文である。

---

【arbiter_mot3 正式回答／P4-1C R2-0再確認(B-required追随規模の前提訂正)+R1/R2有限バンドル判定】

■ 0. 受領確認
- alice報告全文(R1/R2有限バンドル案+§R2-0実測)を読了。基点 `874f8dd9…` 不変。本再エスカレーションは、私の前判定が定めた再エスカレーション条件(「B-requiredの実装で追補B-2想定外の破壊的波及が出た場合」)への正当な該当であり、**実装前に前提の食い違いを実測で申告したaliceの手続きは、この体制の規律(仕様と実測が食い違ったら実測を添えて報告)の正しい履行である。**
- 外部コンテキスト自己申告(HB-DEC-013): 根拠は本報告全文+先行3固定入力+実装基準コードの既読分。system prompt表示モデルはclaude-sonnet-5。

■ 判定(1): **B-requiredを維持する。** 前提コストの訂正(1ファイル→153呼出し、うちtest 143)は結論を変えない。
理由: 私の採用理由は「監査は列挙に依存し、列挙は将来に対して常に不完全。optional既定値は5関数に恒久の供給漏れ穴を開ける」であり、**恒久の穴と一度きりの機械追随の比較は、追随件数が1でも153でも構造的に同じ側に倒れる**(aliceの自己評価「理由付けは規模に依らず成立する」に同意)。153件のうち143件はtestであり、(a)型検査が編集の大半を機械的に強制し、(b)C2移設段階は挙動変更0=**全既存テストが期待値無変更で通過する**という検証面が、編集ミスの検出網としてそのまま機能する。本プロジェクトは同規模の機械追随(P3-4の77ファイル等)を挙動変更0証明つきで完遂してきた実績があり、コストは受容可能である。

■ 判定(2): **S-A(options object)を採用。S-Bを却下する。**
決定的理由は将来コストではなく**編集事故の型安全性**である: S-B(位置引数の途中挿入)では、`coilDeformOmegaRadS`・`loadTorque`・`effectiveInertia`がいずれも`number`であるため、**153件の機械編集中に引数を転置してもTypeScriptは検出できない**(数値同士の位置違いは型互換)。S-Aの名前付きfieldは転置を構文的に構築不能にする——153件という規模だからこそ、編集事故が型で落ちる形式を選ぶべきである。副次的利点(将来の破壊系パラメータ追加が再度の全件追随を要しない・required fieldで省略も構築不能)もS-Aに揃う。alice推奨と一致。

■ 判定(3): 153件追随がエンジン凍結規律に許容される条件(exact、5点)
1. **挙動変更0の機械的証明**: C2移設deltaにおいて、全既存テストが**期待値リテラル無変更**で通過すること。機械追随のdiffに期待値リテラルの変更が1件でも含まれる場合、その時点で停止し再エスカレーション(「呼出し形の変更」と「期待値の変更」を同一deltaに混在させない——再基準化0の機械的検証可能性を保つため)。
2. **P4-0凍結**: `src/p40/__tests__/scenario.test.ts`の13 assert(記録hash3件・aggregate・step数2690/2429/2561・4区間時刻・シナリオ成立)が**1文字も変わらず**通過すること(R2-3案のとおり)。
3. **単一default定数**: bare caller(非test 10件——RaceCanvas.tsxのstepTestRunが別シンボル〈gameStore action〉であるというaliceの注記を確認・採用)が明示的に渡す値・`D01_CALIBRATION.coilDeformOmegaRadS`初期値・旧snapshot補完値の3者すべてが`constants.ts`の`COIL_DEFORM_OMEGA`という**export 1定数**を参照すること(R2-2案のとおり)。
4. **目的の限定**: 本シグネチャ変更の目的はD01状態機械の閾値単一出典化(§2(b))であり、options object化に乗じた他パラメータの意味変更・追加を同一deltaで行わないこと。
5. **実効性fixture**: R2-4案(既定の0.5倍閾値で発火stepと`decayExposureRad`進行が同時に動く単一テスト)を必須として維持(前判定■2(iii)どおり)。

■ 判定(4): R1有限バンドル案——**承認。**
- R1-1/R1-2の明記文案は、前判定のblocking条件**C-1(凍結定義の限定拡張の明記)・C-2(遡及効果の開示)を完全に充足する**。特にR1-2の「強くなる個体は存在しない(tensionPackingRatio≤1)」「legacyローターは影響を受けない」という方向の限定は、開示として要求した水準を超えており適切。
- 257点格子(全ターン同一張力で平均を厳密に格子へ載せる設計)・direction=1固定による張力因子の分離・script-localの有限候補空間20組・単調非減少式(`minPacking + (1−minPacking)×min(1, mean/ref)`、張力0で正値・上限1)——H3/H5・RQ-3の全条件を満たす。
- 再基準化閉包のexact化(2ファイル・6アサーション)と「再基準化不要」の実測根拠3件(recipeCode自動追随・contract test明示指定・p40はaggregate直読)は、前判定の条件(P4-0 aggregate不変の帰結)と整合。
- 停止条件6件・候補選定規則(閾値端張り付き非推奨=P4-0 G3教訓の再適用、推奨1案+非推奨理由の明示)も承認。
- R2-5のC2 sweep(K_LOOSE候補空間・varnished=false固定・varnished=true非漏洩の停止条件・他モード意図しない発火の実測列)も同様に承認する。R1/R2を混ぜない分離も維持すること。

■ 判定(5): 人間再承認文へ追加すべき条件
1. **追随規模の真値の明示**: P41C-R2の人間承認文へ「B-requiredの追随は153呼出し(うちtest 143・非test 10)であり、S-A(options object)形式で行う。当初見積り(1ファイル)からの訂正である」を明記する——人間が承認する対象は実コストを含むため、前提訂正は承認文自体に載せる(無言の差し替えにしない)。
2. 判定(3)の条件1・2(期待値リテラル凍結・P4-0 13assert無変更)を受け入れ条件として承認文へ転記する。
3. R1側は既定どおり: R1-1/R1-2の明記文+sweep結果のexact係数+6アサーションの新値と根拠を同時提示。
4. C2移設は再基準化0、C1反映のみ限定再基準化という分離(前判定■4)を承認文でも区別して書く。

■ 効力
本再判定はP4-1C判定の範囲内の精密化であり、C-1/C-2(blocking)は R1-1/R1-2案で充足見込み(実物提示時にSuu照合で確認)。以後、S-A実装で本判定の条件外の波及が出た場合のみ再エスカレーションとする。production/test実装は各deltaの人間再承認まで禁止継続。arbiter_mot3はコード・docs編集・Git操作を行っていない。

以上、正式回答として全文送信し、以後停止する。

---

## 件数精査後の正式確認

【arbiter_mot3 正式確認】件数訂正(153→135: コメント/文字列内step+gameStore action別シンボル16件の混入除去、正値=18ファイル・135呼出し〈production 4ファイル6・scripts 3ファイル4・test 11ファイル125〉、所有境界越えはp40/sessionRunner.ts 1呼出しのみ・brabit所有UI 0件)を承認します。人間承認文の真値明示は135(+内訳)へ訂正してください——「当初見積り1ファイル→153→精査後135」という訂正の経緯ごと1行で残すこと(数字の差し替えを無言にしない、それだけが条件です)。本訂正は追随範囲の縮小であり、S-A採用・B-required維持・許容条件5点はいずれも不変です(判断根拠は件数に依存しない構造の比較であり、むしろ精査でtest編集が125件へ減ったことは条件1〈期待値リテラル凍結〉の検証を容易にします)。追加条件はありません。コメント・文字列・別シンボルを除外した実呼出し閉包の再実測はpitfalls#2の正しい適用として記録してください。停止を継続します。
