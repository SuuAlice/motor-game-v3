# 開発エージェント向けガイド

This file provides guidance to coding agents working with this repository. `AGENTS.md` and `CLAUDE.md` must always contain the same content.
- 応答・説明・コミットメッセージは日本語で書く
- どのモデルが読んでも本書単独で作業を再開できる水準で書くこと(将来のモデル乗り換え保険。前提知識・用語・落とし穴は本書内で完結させる)

## リレー状態契約
ターン終了前に、リポジトリ直下の .relay/status.json を更新すること:
{"state":"WORKING|WAITING_APPROVAL|DONE|BLOCKED","note":"<現在地1行>"}
- 承認・判断が必要 → WAITING_APPROVAL / 節目の完了 → DONE / 障害 → BLOCKED / それ以外 → WORKING
- .relay/ は .gitignore に追加してよい(ローカル運用ファイル)

## プロジェクトの現状

V3「(仮題)挑め!手作りモーターカーGP」はPhase 0(リポジトリ初期化)・Phase 1(レトロ描画基盤の技術検証、2026-07-22人間承認・完了、`docs/phase1-report.md` §16)が完了しました。本リポジトリは旧V2(「走れ!手作りモーターカー」)の開発リポジトリをそのまま起点とし、**同一履歴上でV3化した**(人間承認済み、`docs/repo-birth-checklist.md`の新規複製ステップは適用していない)。V2は Phase 0〜5が完了済みで、`src/engine/`(モーター物理+車体物理+コース)は206テスト・build・lintがすべて成功した状態で凍結されている(`docs/baseline-v2.0.md`)。GitHub(`suualice08/motor-game-v3`、default branch `main`)・Vercel(project `motor-game-v3`)への分離も完了している(詳細は`docs/handoff.md`)。V3はこのエンジンを基盤に、在庫経済・実在素材ティア・破壊モードと失敗図鑑・実レシピCPUレース・レトロ描画パイプラインを追加する。Phase 2(素材システム: materials.ts+写像層、在庫・個体状態、サルベージ、店)は2026-07-24人間試遊承認・完了した。Phase 3(破壊モード+図鑑)のP3-0(クロスレイヤ契約の型凍結ゲート、`docs/phase3-plan-v12.md`)は2026-08-03人間承認・commit・タグ`p3-0-complete`済みで完了している。P3-1(D01/D03の契約最小実証+store統合、`docs/phase3-p3-1-plan.md`、確定裁定の反映先は`docs/phase3-plan-v12-amendments.md`)は正式Fable最終レビュー承認・人間承認を経て2026-08-08にcommit(`65a765a`)・タグ`p3-1-complete`済みで完了している(pushは未実施)。P3-2(D04〈リポ経路〉+D07〈三段開示骨格〉+store統合、`docs/phase3-p3-2-plan.md` v18、確定裁定の反映先は`docs/phase3-plan-v12-amendments.md`)は正式Fable技術レビュー条件付き承認・Suu_mot3最終照合通過・人間再承認バンドル6項目(`docs/phase3-p3-2-human-reapproval-bundle.md`)+ゲート5較正裁定分1項目(overheated保留規則、2026-08-09T06:20人間再承認、`docs/phase3-plan-v12-amendments.md`のP3-2-Q13-1)の計7項目承認、ゲート0〜9の実装完了、正式Fable最終レビュー承認(2026-08-09、`docs/phase3-p3-2-implementation-report.md`・`docs/phase3-p3-2-final-review-request.md`)・人間commit承認(2026-08-10)を経て、本commitにタグ`p3-2-complete`を付与し完了している(pushは未実施)。spec.md・art-spec.mdはr3(仕様書リビジョン改訂+アート参照資料追加)へcommit `82ccdac`で更新済み。P3-3(D02〈コイル焼損〉+D05〈異常ブラシ火花〉+D01漸減〈P3-1-Q1返済〉+ブラシ素材写像、`docs/phase3-p3-3-plan.md` v17、確定裁定の反映先は`docs/phase3-plan-v12-amendments.md`のP3-3-Q1〜Q15・P3-3-D01較正確定・改訂12)は正式Fable技術レビュー条件付き承認(2026-08-09、Q1〜Q14確定裁定+付帯条件7点)・正式Fable補足裁定(2026-08-10、Q15-1〜Q15-7)・人間再承認バンドル13項目+追補1項目〈Q15-4判別union化、計14項目〉(`docs/phase3-p3-3-human-reapproval-bundle.md`)の人間承認(2026-08-10、「13件を再承認します」+別途「Q15-4再承認します」)を経て実装解禁し、ゲート0〜5(docs同期・型契約・素材写像・状態機械・物理効果・較正sweep)の実装完了、正式Fable補足裁定(2026-08-11、D01較正確定)を経てゲート6(store fixture統合、`src/store/__tests__/runOutcomeApplication.test.ts`の新規10件#71〜#80)へ進み、Suu_mot3独立レビュー5ラウンド(P60〜P64是正、詳細は`docs/phase3-p3-3-plan.md` 13.2.1節)を経て2026-08-11にゲート6が正式通過し(Fable追加裁定不要)、ゲート0〜7完了後、正式Fable最終レビュー(2026-08-10、`docs/phase3-p3-3-fable-final-review.md`、人間プロジェクトリード直接提示・Suu_mot3中継確認済み)がcommit可・タグ`p3-3-complete`付与可と承認した(発効条件1点=D02/D05のtemperature規約〈D02は`{kind:'uncalibratedGauge',ratio:...}`・D05は`{kind:'unavailable'}`〉の実装・テスト確認済み、申し送り追記2点=P3-2-Q9裁定〈`finalDestructionState`/案B/実装P3-4〉の§13第9項目化+P3-4計画起草者への申し送り統合参照義務の明記、いずれも`docs/phase3-p3-3-implementation-report.md`へ反映済み)、Suu_mot3最終照合・人間commit承認(2026-08-10)を経て、commit`96ffc3e`・タグ`p3-3-complete`を付与し完了している(pushは未実施)。**次はP3-4(D06/D09+production配線+人間試遊、Phase 3完成ゲート)の実装前詳細計画の開始指示待ち**の状態にある。production向け`DestructionConfig`・gameStore・UI配線と人間試遊は、P3-0-Q2裁定どおり引き続きP3-4まで行わない。

**上記段落末尾の「次はP3-4」は旧状態であり、以下の最新状態で置き換える。** P3-4は正式commit `4fefebe09f3407910091327d21d08075d0cf9084`・tag `p3-4-complete`を経て完了した。P4-0はG0〜G7を完了し、2026-08-28の人間試遊で入力方式を半自動治具へ確定、表示是正後の代表経路（初走敗北→第2区間修正→二走勝利）と視覚再確認も通過した。正式commitは巻線契約・固定走行基盤`89f9c40`、UI`8f5edcc`、境界監査`b7ddce2`、文書同期`c54c0f1`で、注釈付きtag `p4-0-complete`を付与し、`develop`とtagをGitHubへpush済みである。P4-0はセッション限定・保存なしであり、production物理・較正・schema・既存保存系へは統合していない。P4-1A（巻線記録・ローター個体・save schema 3・recipeKey 2・原子的完成actionの契約凍結）は2026-08-28にexact契約の人間承認、実装、Suu_mot3正式受入まで完了し、99ファイル・2554テスト、build・lint・型検査が成功した。MC4はP4-1Bへ延期し、RunSnapshot 4・D10・被膜消費等は後工程へ残している。**現在はP4-1B（production半自動治具と手続き描画）のexact計画・承認前であり、P4-1Aの正式commit、tag、push、deployおよびP4-1B実装は未承認である。**

- `src/engine/` — 純粋な物理エンジン(`constants.ts`・`commutator.ts`・`motorPhysics.ts`・`vehiclePhysics.ts`・`trackPhysics.ts`・`scoring.ts`・`failures.ts`・`recipeCode.ts`・`__tests__/`)。V2から凍結継承(仕様書§2「エンジン凍結方針」)
- `src/store/`・`src/render/`・`src/components/`・`src/modes/`・`src/data/` — **V2 UI一式。Phase 0時点では凍結参考実装として保持**(削除しない)。V3はレトロ描画基盤(低解像度Canvas)へ全面刷新するため、Phase 1で新UIシェルが構築でき次第、論理単位で置換・削除する
- `scripts/sweep.ts`・`scripts/vehicleSweep.ts` — パラメータグリッド探索ツール。V3でも継続使用

承認済みの `docs/spec.md` をV3における唯一の正とします。V2完了時点の基準は`docs/baseline-v2.0.md`、V2からの引き継ぎ情報は`docs/handoff.md`を参照してください。

コマンド:
- `npm run dev` — 開発サーバー起動
- `npm run test` — Vitestでテストを実行
- `npm run build` — 型チェック(`tsc -b`)+ 本番ビルド
- `npm run sweep` — 車体sweep(`vehicleSweep.ts`)
- `npm run sweep:motor` — モーター単体sweep(`sweep.ts`)

**作業を始める前に、`docs/spec.md`(唯一の正)・`docs/art-spec.md`(UI/描画/音の常時参照物)・`docs/handoff.md`・`docs/baseline-v2.0.md`を読んでください。** 旧資料と実装が矛盾する場合、V3については `docs/spec.md` を優先してください。

## このプロジェクトについて

自分で巻いたモーターを車に載せ、レースで稼ぎ、稼いだ金で実在素材を買い、より速いマシンを作るチューニングレースゲームです。素材は消耗品で、壊せば失いますが、新しい壊し方の発見は失敗図鑑に刻まれ報酬になります(`巻く→走る→稼ぐ→買う→巻く…`のループに`壊す→図鑑登録/サルベージ`が絡む、仕様書§1)。

対象ユーザーは、V1.5/V2から継続する大人のチューニングホビー層です。UIテキストは日本語で、簡潔・正確に、単位を省略せず書く必要があります(仕様書§7.4相当、詳細は`docs/art-spec.md`)。「答えを教えない、生の数値を見せる、現象は隠さないが原因は特定させる」という難易度哲学はV1.5から継承します(仕様書§1.2)。

## アーキテクチャ

設計の核となる原則は、V2から変わらず**物理エンジンを React/DOM から完全に切り離す**ことです。物理エンジンは `src/engine/` 内に純粋な TypeScript 関数として実装します。

### エンジン凍結方針(仕様書§2)

V2で確立した`src/engine/`の構造(二段API `evaluateMotorFrame`/`advanceMotorState`・`energyUsedJ`の正統性・決定論とシード)は凍結する。V3での拡張は次の5点に限定する:

(a) **素材写像層**: `materials.ts`(データ)+`materialMapping.ts`(素材→既存パラメータへの純関数写像)を新設。`engine/`本体は素材を知らない(Phase 2)
(b) **破壊モードの状態機械**: D01〜D10(仕様書§7.1)。演出タスクではなくエンジンの状態機械として実装する(Phase 3)
(c) **破壊イベントのコールバック**: (b)の状態遷移をUI層へ通知するフック(Phase 3)
(d) **整流モデルの進角拡張**: 仕様書§9.2の整流子工程(溝掘り)に対応。現行`commutator.ts`はθ=0/πの固定角比較のみで進角オフセットを持たない。改修規模はPhase 2末までにaliceが見積り、大規模なら進角は将来枠へ降格する(2026-07-23: 見積り完了・大規模と判定・将来枠へ降格確定。詳細はdocs/phase2-step10-advance-angle-estimate.md)
(e) **車両層の周回・横方向拡張**: 仕様書§6.5の周回コース(曲率プロファイルκ(s)・コーナー保持判定・壁擦り)に対応。改修は車両層に閉じ、モーター物理は不変。規模はPhase 2末までにaliceが見積る(2026-07-23: 見積り完了、中〜大規模として採用。Phase5で(e)-1周回構造→(e)-2壁擦り+ローラーに分割。各実装は別計画・Suu/Fable/人間承認を要する)

上記5点以外の目的でengine/の構造(二段API・energyUsedJ・決定論)を変更しないこと。

### 物理モデル(V2から凍結継承)

物理エンジンは**モーター層**(v1.5をそのまま継承)と**車体層**の二層構成。固定タイムステップ dt = 1/120s で積分する(描画は60fps、1フレームあたり物理ステップは最大2回)。

モーター層の中心方程式:
```
J_motor · dω_motor/dt = T_mag + T_cog + T_brush + T_drag − T_load
```

車体層(非空転時、反射慣性方式):
```
J_eff · dω_motor/dt = T_mag + T_cog + T_brush + T_drag + T_resist_reflected
```
**モーター軸と車軸を別々の自由度として積分してはならない。** ギヤの運動学的拘束 `ω_axle = ω_motor / G` を常に満たすよう、車体質量をモーター軸へ換算した反射慣性`J_eff`で一つの運動方程式として解く。空転時(駆動力要求がグリップ上限を超えた場合)だけモーター側と車体側を2自由度に分離し、再結合時は車速を不連続に変えず、消えた回転エネルギーをタイヤの滑り損失として記録する(総エネルギーを増やさない)。詳細な式・落とし穴は`docs/handoff.md`を参照。

### 素材→物理パラメータの写像(仕様書§4、Phase 2)

すべて実在素材とし、物性値はカタログ値に準拠する。架空素材・架空物性は導入しない。写像は起動時に一度計算し、走行中は既存パラメータのみが生きる(決定論の維持)。摩耗・減磁などの恒久状態はアイテム個体に持たせ、エンジンには毎回、劣化込みの実効値を渡す。

### 破壊モードとパラメータの対応(仕様書§7)

画面上に現れる破壊症状(D01〜D10)は、必ず実物の工作・走行で起こりうる原因と対応させる(仕様書§7.1の表)。この対応関係はシリーズ共通の教育的価値の核心であるため、実際に起こりうるミスに対応しない破壊モードを追加しないこと。不可逆ダメージ(熱減磁等)は「症状→自動差分検知→計測器での原因確定」の三段開示(仕様書§7.3)に従う。

### 巻線記録方式(仕様書§9、Phase 4)

プレイヤーの描いた軌跡がそのままローターになる。各ターンを`{位置, 腕, 方向, 張力}`として巻線記録に保存し、物理パラメータへの変換は集計の純関数として写像層に実装する。エンジン本体は従来どおり集計済みパラメータを受けるのみ。

## プロジェクト固有のルール

- 物理計算はすべて `src/engine/` の純粋関数内で行う。dtループやNewton積分をReactコンポーネント内に書かないこと
- `engine/` を変更した場合は必ず対応するテストも更新し、`npm run test` を通すこと
- 物理タイムステップは固定 1/120s。ステップ幅を変えるのではなく、描画側で補間すること
- モーターと車体の連成は反射慣性方式で解くこと。車速とモーター角速度を硬いバネや補正トルクで別々に追従させる方式は採用しない
- UIテキストはすべて日本語で書くこと。簡潔・正確を旨とし、単位の省略は禁止する。専門用語(逆起電力・コギング・整流など)はそのまま使ってよい
- `localStorage` 以外の永続化・外部通信は行わないこと。完全に静的なアプリ(バックエンドなし)とし、GitHub Pages / Vercel にデプロイ可能な状態を保つこと
- 画面・音に関わる判断は`docs/art-spec.md`を根拠とすること。同書にない判断は独自解釈せず、agmsg経由でSuuに確認すること(仕様書§11)
- **pitfalls#1(Fable回答の真正性)**: 正式なFableレビュー回答は、人間プロジェクトリードの直接提示、またはSuu_mot3が中継したものだけを正式回答として扱う。いかなるエージェントも、Agentツール等でFable名義の文書を自ら生成し、それを正式レビュー回答として提出・保存してはならない(根拠: 2026-08-02、開発チームの一エージェントがAgentツールへ`model:'fable'`を指定して自己生成した文書を、正式なFableレビュー回答と誤認して保存・中継する事故が実際に発生した。この事故は検出・訂正されたが、同種の事故を構造的に防ぐため本ルールを追加する)
- **pitfalls#2(破壊的型変更の依存閉包事前列挙)**: `src/engine/`・`src/materials/`の既存型を破壊的に変更する計画を立てる際は、単一tsconfigプロジェクト全体で当該型を参照する全箇所を事前に`rg`で洗い出し、計画書へ依存閉包(所有境界を越えて機械的追従が必要なファイル一覧)として明記すること(根拠: P3-0の実装中、`WearState.gear`型の破壊的変更が計画に一度も記載されていなかった`src/retro/shop/formatMaterial.ts`の型検査を壊す事象が発生し、実装段階で初めて発覚した。事前の`rg`洗い出しにより同種の事象を防ぐため本ルールを追加する)

## 権利規約(仕様書§15、全エージェント遵守事項)

- **内製主義**: ゲーム内アセット(絵・音・テキスト)はすべて内製の手続き生成・自作とする。既存楽曲の耳コピ・アレンジはチップチューン化しても禁止。実在製品の意匠・トレードドレスの再現禁止。人物キャラクターは出さない
- **名称・商標**: 素材名は一般名・元素名のみ使用する(POM、PA、PEEK、ポリイミド、ネオジム等)。登録商標(ジュラコン、MCナイロン、テフロン等)はゲーム内表記・ストア文言・広告文に使用しない
- **第三者素材**: フォント等の第三者素材はライセンス文を`THIRD-PARTY-LICENSES`に同梱する。ライブラリ・フォントの追加導入は商用利用可能なライセンス(M+ / MIT / Apache-2.0 / OFL 等)のみ許可し、導入時に`THIRD-PARTY-LICENSES`へ追記すること(UI担当の責務)

## 非機能要件

- ミッドレンジスマホを基準に60fps描画を目標とする。物理ステップは1フレームあたり最大2回まで。遅延時は時間を飛ばさず描画品質を下げる
- 初回ロードは1MB未満。画像アセットではなくCanvas描画を使うこと(V3はSVGではなく低解像度Canvasネイティブドット描画、仕様書§8)
- スライダー・タブ・スタート操作はキーボード操作可能にすること。色だけに依存した状態表示をしないこと
- 依存パッケージは最小限に保つこと。理由なく他の依存を追加しないこと
- 実在するミニ四駆・実在製品の商品名・ロゴ・車体デザインを使用しないこと。電池の短絡・発熱・工作道具について現実の安全注意を表示すること

## 実装フェーズ(仕様書§12)

Phase 0(alice、完了): リポジトリ初期化(同一履歴上、案A)・baseline文書・仕様書配置・AGENTS.md/CLAUDE.md/docs/handoff.mdの整備・THIRD-PARTY-LICENSES新設・GitHub/Vercel分離。

| フェーズ | 内容 | 主担当 |
|---|---|---|
| Phase 1 | レトロ描画基盤: 低解像度Canvas・整数拡大・俯瞰斜め3/4走行ビュー・解像度試作比較・スーファミ級の技術検証 | UI |
| Phase 2 | 素材システム: materials.ts+写像層、在庫・個体状態、サルベージ、店(経済数値は仮) | alice(写像)+UI(店UI) |
| Phase 3 | 破壊モード+図鑑: 状態機械(D01〜D09)、パーティクル演出、検死レポート、三段開示 | alice(状態機械)+UI(演出) |
| Phase 4 | ガレージ深化: 巻線記録方式一式、整流子工程、被膜ダメージ蓄積、D10。進角のエンジン対応(Phase2末見積りで採否確定済み) | UI(入力・描画)+alice(写像・進角) |
| Phase 5 | レース&経済: 周回コース(§6.5、車両層拡張(e)はPhase2末見積りで採否確定済み)、実レシピCPU、ボス・解禁、賞金・図鑑報酬の経済結線、4体目投入 | 全員 |
| Phase 6 | 仕上げ: BGM/SE拡充、CRTオプション、スマホ最適化、デプロイ | UI |

各フェーズ末に人間の試遊承認。共通DoDは `npm run test && npm run build && npm run lint` の成功。物理エンジンを変更した場合は対応する数値テストを必ず追加すること。**実装前に計画を提示し、承認後に着手する。エンジンの計画は実装承認前にFableのレビューを経る。**

## マルチエージェント開発体制(仕様書§11より)

| 役割 | 担当 | 責務 |
|---|---|---|
| エンジン | **alice**(Claude Code) | `src/engine/`オーナー。素材写像・破壊状態機械・経済フック・sweep検証。V1.5以来の規律(構造凍結・定数逆算)を維持 |
| UI/描画/音 | **brabit**(Claude Code) | レトロ描画パイプライン・ガレージUI・パーティクル・Web Audio。`docs/art-spec.md`を常時参照 |
| マネージャー | **Suu**(Codex/GPT-5.6) | 実装から離れ、計画レビュー・進捗整理・agmsg中継に専任 |
| コンテンツ(後発) | **4体目**(Claude Code、Phase 5で投入) | ボスレシピ設計・素材テーブル調整・経済バランスsweep。sweep基盤を使うがengine/本体は触らない |
| 物理・アーキテクチャレビュー | Fable(Claude) | スプリント境界のレビュー、物理判断、エージェント間の裁定 |
| 承認者 | プロジェクトリード(人間) | スプリント境界の承認、試遊、リポジトリ同一性・命名・外部サービス連携等spec §11の最終確定事項 |

- 通信は agmsg(チーム: MotorGameV3)。**レビュー条件・承認条件は要約せず全文中継する**(V2での教訓)。人間はスプリント境界の判断のみ行う
- 役割境界: `src/engine/`への変更はaliceのみがコミットする。エンジンAPIの変更が必要な場合はagmsgでaliceに依頼し、aliceがテスト込みで実装する
- コミットメッセージに担当エージェント名を含める(例: `feat(engine): 素材写像層の実装 [alice]`)
- **`CLAUDE.md` と `AGENTS.md` は常に同内容に保つこと。** どちらかを更新したら必ず他方も同じ内容に更新する(`cmp AGENTS.md CLAUDE.md`で確認)
